spring.application.name=auth-service
# Import centralised config from the config-server when it is reachable; optional: so the
# service still boots standalone during local development if the config-server is down.
spring.config.import=optional:configserver:http://localhost:8888
